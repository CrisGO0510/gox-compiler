{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Ejecutar Extensión",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/gox-runner"
      ],
      "outFiles": ["${workspaceFolder}/gox-runner/out/**/*.js"],
    }
  ]
}
